@AGENTS.md

# Working Agreements & Persistent Decisions

> Loaded every session. Last updated 2026-07-11 (SP6/SP7/SP8 in prod; SP13 security batch 1 shipped to prod; SP14 SEO/GEO/AEO spec approved; README replaced + git history rewritten to Allen's identity + force-pushed; see "Session state" below).

## Session state — 2026-07-11 (pre-compact snapshot)
- **SP13 batch 1 (SEC-01/02/08/09): ✅ SHIPPED TO PROD & VERIFIED.** Migration `20260706000002_sp13_rls_hardening.sql` applied to prod (`ahgmszdrhndcycvairmn`) + staging; prod functional matrix passed (anon roster read denied even with real admin profile; self-read + service-role intact; `reviewed_by`/`reviewed_at`/`confidence` hidden from anon). Merged `staging`→`master` (rewritten head `f0962cd`), prod deploy healthy. **Batch 2 (SEC-03, SEC-04, SEC-05, SEC-06, SEC-07): NOT STARTED.**
- **SP14 (SEO + GEO/AEO): SPEC APPROVED** (`docs/superpowers/specs/2026-07-06-sp14-seo-geo-aeo-design.md`) with the recommended calls: **canonical = country-neutral** title URL (region is UI state, no per-country indexable pages/hreflang); **allow answer crawlers only** (GPTBot/OAI-SearchBot, ClaudeBot, PerplexityBot, Applebot-Extended) — not pure-training crawlers; **suppress `WatchAction`/answer text for `low`-confidence availability**; **poster-URL OG images** (generated cards deferred). **Implementation plan NOT yet written.** Must land before SP10.
- **Repo identity rewrite (DONE):** `README.md` replaced with the public project overview; **entire git history rewritten** (git-filter-repo) so every commit author+committer across all branches = `Allen Montellano <allenmontellano@gmail.com>` (content/messages/timestamps unchanged; verified on GitHub, `gh_author_login: allenmontellano`). Force-pushed `master`+`staging` with `--force-with-lease`. **Rollback safety still local:** `backup-before-rewrite` branch + `../wciwi-pre-rewrite-backup.bundle` + `../wciwi-pre-rewrite-heads.txt` — **safe to delete once the GitHub contribution graph is confirmed correct.** Local git config now `user.email=allenmontellano@gmail.com`.
- **Backlog scope clarifications (given this session, NOT yet built):** (1) **Report button** = a **persistent per-region-row** report affordance **+ a zero-results-view** report entry point (currently only per-title/search-result modal). (2) **Favicon** = replace placeholder with a **real logo + apple-touch-icon + web manifest** set. Both now scoped (were flagged "unclear" in BACKLOG.md).
- **Backlog doc `docs/superpowers/BACKLOG.md` is current** as of this session (tracked, ordered by impact). **Open TODO:** `Project Han.md` (untracked, gitignored) still holds the **decisions table + platform-coverage matrix** — fold those unique sections into a tracked doc before deleting the scratch file.
- **Launch sequence (next):** SP13 batch 2 → SP11 + MOTN decision (**leaning: don't renew MOTN, revisit only if users complain**) → SP12 → SP10 E2E gate → launch flip (`SITE_INDEXABLE`→true, prod only).

## Auto mode (working agreement)
- **Claude Code may write, edit, run tests, and commit to FEATURE BRANCHES without asking** for per-action approval. Work to completion, then report.
- **Still pause for explicit go-ahead on:** merges to `staging` or `master`, database migrations, and any decision requiring product/architecture judgment.
- Spec and plan approval gates still apply (brainstorm → spec → plan → build) — auto mode covers the *build* loop, not the design gates.

## Current state (2026-06-30)
- **SP4 SHIPPED TO PROD.** Merged `staging` → `master` (`09eaef3`); production now runs functions in **`sin1`** co-located with Supabase + Upstash (verified `X-Vercel-Id: sin1::sin1`, search returns results, homepage 200, 227 tests green). The `sin1` fix, SP4 load-test tooling, and Phase 3 GO report are all live in prod. Load test (on staging): **cached p95 32ms ✅, DB p95 231ms ✅**, TMDB cold-seed advisory/accepted (~3.5s).
- **Staging redeployed; rate limit restored.** `RATE_LIMIT_SEARCH` deletion is now live — staging search verified back at the default **30/min** (burst test: exactly 30×200 then 429).
- **SP6 (Auth & roles, invite-only) — ✅ SHIPPED TO PROD (2026-06-16).** Spec `docs/superpowers/specs/2026-06-16-sp6-auth-roles-design.md`, plan `docs/superpowers/plans/2026-06-16-sp6-auth-roles.md`. 9 tasks + invite-confirm fix built TDD via subagents with spec+quality review; **266 tests green**, tsc + eslint clean. Live-verified on staging AND prod (login/account/logout/route-guard/escalation-block/invite-accept). See PROD ROLLOUT bullet below.
  - **Decisions:** email+password with invite onboarding (`inviteUserByEmail`); invites via dashboard + `scripts/invite.ts` (no in-app admin UI → SP7); **Approach A** `profiles.role` enum (`contributor`|`reviewer`|`admin`) + app-layer `requireUser()`/`requireRole()`; password-reset deferred; profile row created in `/accept-invite` server action via admin client.
  - **Built:** `lib/auth/roles.ts`, `lib/auth/guards.ts`, `lib/auth/accept-invite.ts`, `lib/supabase/client.ts`, `lib/supabase/proxy-session.ts` + root **`proxy.ts`** (Next 16 renamed `middleware`→`proxy`), `app/login`, `app/accept-invite` (+action), `app/account` (+logout), `scripts/invite.ts`, migration `supabase/migrations/20260616000001_profiles_role.sql` (role enum + anti-escalation trigger; **idempotent + JWT-claim-safe** after review).
  - **STAGING GATE (2026-06-16): FULLY VERIFIED incl. invite-accept fix; PROD GATED.** `feat/sp6-auth` merged → `staging` (`af9c485`); invite fix merged (`82b1961`); deploy live at staging.wherecaniwatchit.info. Migration applied+verified on staging; **prod `ahgmszdrhndcycvairmn` untouched**. Token rotated by user. **Live staging verification results:**
    - ✅ **WORKS:** `/login` renders; **login → `/account` → logout** full UI flow (Playwright, real browser) — `/account` shows username/email/role; logout clears session (`/account`→307→`/login`); unauth `/account`→`/login`; static `_next` assets + favicon load (proxy matcher OK); **escalation block CONFIRMED** — authenticated user PATCH `profiles.role=admin` via PostgREST → **400 "role cannot be changed by a client"**, role unchanged (trigger fires on live staging).
    - ✅ **FIXED — invite onboarding now works (verified end-to-end on staging).** Root cause was the implicit/hash flow (tokens in URL hash, invisible to SSR). **Fix = Approach A:** added `app/auth/confirm/route.ts` (GET handler: `verifyOtp({type,token_hash})` sets the session cookie via the server client, then `redirect(next)` with an open-redirect guard rejecting `//` and `/\\`). Built TDD (7 tests), reviewed (open-redirect backslash bypass caught + fixed). Merged `fix/sp6-invite-confirm` → `staging` (`82b1961`). **Verified live (Playwright):** generated a real invite `token_hash` → `/auth/confirm` exchanged it → `/accept-invite` (session live) → set username/password/region → `/account` shows role **reviewer** (from `app_metadata` → `profiles.role`). 266 tests green.
    - ⚠️ **REQUIRED CONFIG before REAL email invites work (Supabase dashboard, user action):** the "Invite user" email template must point its link at `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=invite&next=/accept-invite` (default template uses `{{ .ConfirmationURL }}` = the broken hash flow). Verified via `generateLink` (bypasses the template), so the *code* is proven; the template edit is needed on **staging** (optional re-test with a real email) and **prod** (required before prod invites). `scripts/invite.ts` needs no change.
    - **PROD ROLLOUT (2026-06-16): ✅ SP6 SHIPPED TO PRODUCTION.** (1) ✅ prod "Invite user" email template → `token_hash` pattern (user); (2) ✅ migration applied + verified on prod (`ahgmszdrhndcycvairmn`) — `profiles.role`, enum, trigger, recorded; (3) ✅ `NEXT_PUBLIC_SITE_URL=https://wherecaniwatchit.info` on prod Vercel; (4) ✅ merged `staging` → `master` (`e8f0cf3`, pushed), prod deploy live; (5) ✅ **prod spot-check PASSED** (Playwright, real browser on www.wherecaniwatchit.info): `/login` renders, login → `/account` shows role **reviewer**, logout → `/login`, unauth `/account` → `/login` route guard. `/auth/confirm` live (307→/login w/o token). Only console noise = Cloudflare Insights beacon DNS fail (unrelated). Access token `sbp_5ede…` rotated by user. **SP6 COMPLETE.**
  - **Deferred follow-ups (non-blocking, logged):** (1) guards log auth-service errors (currently fails closed silently); (2) proxy-session unit test for `setAll` cookie propagation; (3) invite CLI partial-failure message should include user id. *(Region-validation + form autocomplete/aria-label already fixed pre-gate in `926f4df`.)*
- **SP8 (expanded reporting form) — ✅ SHIPPED TO PROD (2026-06-30).** Spec `docs/superpowers/specs/2026-06-26-sp8-expanded-reporting-design.md`, plan `docs/superpowers/plans/2026-06-26-sp8-expanded-reporting.md`. 6 tasks built TDD via subagents (spec+quality review each) + modal error-surfacing fix; **280 tests green**, tsc + eslint clean. Staging + prod verified live (Playwright). See PROD bullet below.
  - **Built:** migration `20260626000001_flags_reported_columns.sql` (adds `reported_platform` + `reported_watch_url`, additive); `lib/flags.ts` `sanitizeWatchUrl` (origin+pathname, http(s)-only) + `sanitizePlatform` (known slug or ≤100-char charset-safe name; hyphen-literal verified) and **`composeNotes` removed**; `lib/platforms-data.ts` (region→platforms map + region slug-set); `/api/flags` stores structured fields + server-side required-platform; report modal platform **dropdown + "Other" + watch-URL**; wired `platformsByRegion` from BOTH title page and search results (2nd ReportModal caller the plan missed).
  - **STAGING VERIFIED (2026-06-30) — AWAITING SEPARATE PROD APPROVAL.** Migration `20260626000001` applied+verified on **staging** (`hunvbflchgjphnhdjmws`; both columns text/nullable, recorded); **prod `ahgmszdrhndcycvairmn` untouched** (no reported columns). `feat/sp8-reporting` merged → `staging` (`37c9750`), deploy live. **Live browser verification (Playwright) PASSED:** platform dropdown lists exactly the region's 9 PH platforms + "Other"; `is-here` with no platform → modal shows inline *"A platform is required for this report."* (no false success — the error-surfacing fix works); "Other"=`iWant-TFC` stored as `iWant-TFC` (sentinel correctly mapped, not stored literally); watch URL `…?utm_source=share&si=abc#play` stored **stripped** → `https://www.iwanttfc.com/title/obsession-2026`; structured columns correct. Test flag cleaned up.
  - **UX fix applied (`02b564e`):** report modal now surfaces API validation errors (non-2xx → show the error, let the user correct) instead of a false "Report submitted"; network/transient failures stay best-effort.
  - **PROD ROLLOUT (2026-06-30): ✅ SP8 SHIPPED TO PRODUCTION.** (1) ✅ migration `20260626000001` applied + verified on **prod** (`ahgmszdrhndcycvairmn`; both columns text/nullable, recorded; prod was `ACTIVE_HEALTHY`); (2) ✅ merged `staging` → `master` (`98729e9`, pushed), prod deploy live; (3) ✅ **prod spot-check PASSED** — `/api/flags` validation live (`400 "Invalid watch URL."` pre-insert probe), and the report modal on www.wherecaniwatchit.info (PH) renders the platform **dropdown** with all 9 PH platforms + "Other". Access token `sbp_b8d1…` shared in-session — **rotate.** **SP8 COMPLETE.**
  - **Deferred follow-ups (non-blocking):** silent error swallowing in `platforms-data` fetch wrappers; `platformOther` not reset on switch-away; a few boundary tests; SP7 reviewer note on the slug-or-text `reported_platform` dual interpretation.
- **Branch state (2026-06-30):** `master` (= prod) and `staging` both at the SP8 merge (`master` `98729e9`, `staging` `37c9750`+docs) — **no pending prod deploys.** Feature branches `feat/sp6-auth`, `fix/sp6-invite-confirm`, `feat/sp8-reporting` already merged (safe to delete). Next dev branches from `staging`.
- **NEXT UP — SP7 (Internal CMS & Data Accuracy Layer): the critical-path item** once SP8 ships to prod. SP7 consumes SP6 auth (Reviewer/Admin roles + `requireRole`) and SP8's structured `flags` columns (review queue). **Workflow note (Allen):** Allen wants to design SP7's UI in **Claude Design** *before* implementation — so Claude Code must first produce the **SP7 technical spec** (data model, review-queue states, role-gated actions, API surface, the Disney+ PH/`motn` low-confidence rule) to inform that design session. So SP7 sequence = brainstorm → **technical spec (hand off to Claude Design for UI)** → plan → build. (Contribution counter + admin view first; payment rate deferred — see Pre-launch product decisions.)

## SP7 build progress (branch `feat/sp7-cms`; update after EVERY phase — session-resilience protocol)
- Spec: `docs/superpowers/specs/2026-07-06-sp7-cms-data-accuracy-design.md` (approved; Fable-ready; build order §13). Design approved in-session 2026-07-06; **build authorized straight from spec (no separate plan doc)** due to the July-7 Fable window.
- **Phase 1 ✅ (2026-07-06):** `lib/confidence.ts` + 10 TDD tests (Disney+ PH rule keys on `source∈{api,cron}` + slug `disney` + PH — NOT 'motn'); migration `20260706000001_sp7_cms.sql` **written, NOT applied** (confidence enum + default medium + backfill of the 26 prod Disney+ PH aggregator rows → low; `titles.metadata_overrides` jsonb; `tmdb_id` nullable; flag review columns); `/admin` shell (`app/admin/layout.tsx` requireRole all 3 roles, `components/admin/admin-nav.tsx` role-filtered nav, overview page w/ own contribution count). Types extended (Availability.confidence/reviewed_*, Flag.reported_*/reviewed_*/resolution). `tmdb_id: number|null` TYPE change deferred to Phase 4. 290 tests green, tsc clean. Commits: 0812493 (spec), 8789109, e67b05a, beb54ac.
- **Phase 2 ✅ (2026-07-06):** review queue live in code — `lib/admin/flags-service.ts` (acceptFlagCore/rejectFlagCore, 8 TDD tests: confidence per actor, provenance, contribution RPC, cache drop, URL sanitize, platform-not-found) + `lib/admin/queue-sort.ts` (risk-first sort, 5 tests) + `app/admin/queue/` (page + server actions wiring requireRole/createAdminClient/revalidatePath) + `components/admin/queue-list.tsx` (pre-filled confirm panel: platform select preselected from reported slug, watch-URL, available checkbox defaulting from issue_type, reject). `increment_contribution(p_user_id,p_n)` RPC **added to the unapplied migration**. Nav entry added. NOTE: react-hooks/purity lint forbids `Date.now()` in component render — use absolute dates or pass timestamps from data. 303 tests green, tsc + eslint clean. Commits: f32942b, a386234.
- **Phase 3 ✅ (2026-07-06):** availability editor — `lib/admin/availability-service.ts` (writeAvailabilityCore/confirmAvailabilityCore, 6 TDD tests; **acceptFlagCore refactored to reuse writeAvailabilityCore** — one shared write path), `/admin/availability` (ilike title search → per-region grid: existing rows w/ confidence+source badges, edit available/watch-URL, add-platform row), `/admin/pending` (reviewer/admin-only: `confidence='medium' AND source='contributor'` rows → "Confirm → high" sets provenance + contribution). Nav entries added (Pending approval hidden from contributors). 309 tests green, tsc + eslint clean. Commits: ec63e13, b9e33a0.
- **Phase 4 ✅ (2026-07-06):** title management — `lib/admin/title-overrides.ts` (stripOverriddenKeys/overrideWarning, 8 tests), **`syncTitle` now strips overridden keys on EVERY path** (search on-demand + cron + admin re-sync all safe; sync.test.ts extended), `Title.tmdb_id` → `number|null` + `metadata_overrides` in types (fallout fixed; cron filter skips null-tmdb_id local titles), `lib/admin/titles-service.ts` (save/reset overrides, add local title; 6 tests), `/admin/titles` (search + AddTitleForms: by-TMDB-id via syncTitle cast + local) and `/admin/titles/[id]` (TitleEditor: 7 fields, per-field overridden badge + reset-to-TMDB, stable/airing warning banners, re-sync button hidden for local). Nav 'Titles' (admin). 324 tests green, tsc + eslint clean. Commits: 683e-series → 838278f.
- **Phase 5 ✅ (2026-07-06):** admin conveniences — `lib/admin/users-service.ts` (changeRoleCore w/ **last-admin demotion block**, inviteUserCore matching scripts/invite.ts mechanism; 6 tests), `lib/admin/platforms-service.ts` (validatePlatformInput: slug regex, region whitelist; 6 tests), `/admin/platforms` (create+edit, slug immutable, region toggle picker), `/admin/users` (invite email+role, role dropdown w/ optimistic revert on error), `/admin` upgraded to admin dashboard (own count for all roles; admins also get pending-reports/awaiting-approval/confidence-distribution stat cards incl. low-confidence Disney+ PH widget + per-user contribution table). Nav complete. **ALL 5 PHASES DONE.** 336 tests green, tsc + eslint clean, `next build` passes (8 /admin routes). Commit: 5c1fa0b (+ users-service commit before it).
- **✅ SP7 SHIPPED TO PRODUCTION (2026-07-06).** Prod migration `20260706000001` applied + verified on `ahgmszdrhndcycvairmn` (all 8 objects; **26-row Disney+ PH backfill exact** — 26 low / 9,094 medium; migration recorded). `staging` → `master` merged (`ba9534f`, pushed), prod deploy live, 336 tests green pre-push. **Prod spot-check PASSED:** all 7 /admin routes (+2 dynamic children) return 307→/login (route exists + guard works, no 404/500); homepage + search still 200 (no regression); dashboard numbers cross-checked vs direct DB (pending 0, awaiting 0, confidence 26 low/9094 medium, profiles 0). **OPERATIONAL NEXT STEP for Allen:** prod has **0 profiles** — invite yourself as admin (`scripts/invite.ts` or Supabase dashboard) before the CMS is usable; routes guard correctly but there's no admin login yet. **Token `sbp_5df7…` used for prod migration — ROTATE.** SP7 COMPLETE.
- **STAGING GATE ✅ FULLY VERIFIED (2026-07-06) — [now superseded: shipped to prod above].** (1) Staging was ACTIVE_HEALTHY; migration `20260706000001_sp7_cms.sql` applied + recorded via Management API on `hunvbflchgjphnhdjmws`; all 8 object checks pass (confidence enum+default, backfill → staging had exactly **1** Disney+ PH `api` row now `low` (prod will be 26), metadata_overrides jsonb, tmdb_id nullable, flag review columns, increment_contribution fn, index, migration recorded); **prod `ahgmszdrhndcycvairmn` confirmed untouched** (no confidence column, no migration record, tmdb_id still NOT NULL). (2) `feat/sp7-cms` merged → `staging` (`b9f12ab`), 336 tests green post-merge, deploy live. (3) **Live Playwright verification PASSED across all 8 /admin routes** (test admin user): dashboard stats live (pending 1 / awaiting 1 / low 1 = the backfill row, per-user table); queue accept = pre-filled confirm (Viu preselected from slug, URL prefilled) → availability `source=reviewer, confidence=high, reviewed_by set`, flag `resolved/accepted`, contribution 0→1; pending-approval Confirm → contributor row `medium→high` + provenance, contribution→2; availability grid renders all 5 regions w/ confidence badges; title override save → column + `metadata_overrides` key + "overridden" badge + stable warning banner ("Released"), contribution→3; per-field reset → key removed; platforms page (23 rows + create form); users page (invite form, "(you)", role dropdown); **last-admin self-demotion BLOCKED live** ("Cannot demote the last admin", role unchanged); **trust ladder confirmed live** — demoted to contributor: `/admin/titles`→307→`/account`, nav collapsed to Overview/Queue/Availability, dashboard shows own count only. All test fixtures cleaned (user, flag, availability rows, override, network value restored). **Token `sbp_b8d1…` used this session — ROTATE.**
- **NEXT (separate explicit approval): PROD ROLLOUT** — (1) apply migration to prod `ahgmszdrhndcycvairmn` (expect 26-row backfill; verify all objects + record); (2) merge `staging` → `master`, deploy; (3) prod spot-check (login, /admin dashboard, queue empty-state, low-confidence widget = 26). Fable window note no longer gating — SP7 is staging-complete.
- **Gates pending:** migration → staging (Allen approval), staging merge (Allen approval). July-7 deadline does NOT override gates — stop at a gate if access closes.

## SP13 — Security review + fixes (branch `feat/sp13-security-batch1`)
- **Review doc:** `docs/superpowers/specs/2026-07-06-sp13-security-review.md` — full-app independent review; 9 findings (2 Medium, 5 Low, 2 Info), no Critical/High. Four live checks (error-disclosure, auth-enforcement, service-role enumeration, git-history secret scan) all PASSED (§7).
- **Batch 1 built (TDD, 2026-07-06) — AT STAGING GATE:** SEC-08 gitignore artifacts (`84cbc21`); SEC-09 fail-fast timeouts — `lib/with-timeout.ts` + wrapped getTitleDetail/flags-insert (8s) + cache Redis ops (3s, fail-open) (`337c00c`); SEC-02 `lib/auth/with-role.ts` wrapper + `admin-surface-guard.test.ts` CI check + all 5 admin action files migrated off inline requireRole (`cd29002`); SEC-01 **migration `20260706000002_sp13_rls_hardening.sql` WRITTEN, NOT APPLIED** (`b06617a`) — drops `profiles_public_read`, adds self-read + reviewer/admin JWT-claim read; revokes `reviewed_by`/`reviewed_at`/`confidence` on `availability` from anon/authenticated (app uses service role, unaffected). 353 tests green, tsc + eslint clean, `next build` passes.
- **STAGING GATE ✅ VERIFIED (2026-07-06) — PROD PENDING SEPARATE APPROVAL.** Migration `20260706000002` applied+recorded on staging `hunvbflchgjphnhdjmws` (ACTIVE_HEALTHY). Schema verified: profiles has only `self_read`+`staff_read` (no public_read); availability anon grants = 11 safe cols, `reviewed_by`/`reviewed_at`/`confidence` absent; no table-level anon grant. **Functional RLS matrix PASSED** (real anon key + signed-in user JWT + service role): anon reads profiles → `[]`; contributor reads profiles → only own row (self-read scoping); service role → all; anon select reviewed_by/confidence → 42501 permission denied; anon safe cols → OK; service role sees all cols. **Prod confirmed untouched** (no migration, `profiles_public_read` still present). Merged `feat/sp13-security-batch1` → `staging` (`3543410`), 353 tests green, deploy live. **Live /admin (Playwright) PASSED:** login works (proves getSessionUser self-read under new RLS), dashboard renders (137 medium/1 low stats via service role), platforms page 23 rows, `createPlatform` withRole action executed live + returned validation error (no pollution). All test fixtures cleaned. **Token `sbp_beba…` used — ROTATE.**
- **✅ BATCH 1 SHIPPED TO PROD (2026-07-06).** Migration `20260706000002` applied+recorded on prod `ahgmszdrhndcycvairmn` (schema verified: profiles self_read+staff_read, no public_read; availability anon grants exclude reviewed_by/reviewed_at/confidence). **Prod functional matrix PASSED:** anon reads profiles → `[]` even though the real `allenmontellano` admin profile exists (roster hidden on prod); service role sees it; anon select reviewed_by/confidence → 42501 denied; anon safe cols OK; throwaway signed-in contributor self-read → only own row (deleted after). Merged `staging`→`master` (`81510c0`), 353 tests green, prod deploy healthy (home/search 200, /admin 307, anon roster read denied post-deploy). **Token `sbp_beba…` — ROTATE.** Batch 2 (SEC-03..07) still deferred.
- **Batch 2 (deferred, not started):** SEC-03 (sanitize watch_url on MOTN path + at render), SEC-04 (allowlist title-override columns), SEC-05 (dedicated IP_HASH_SALT), SEC-06 (trusted client-IP source), SEC-07 (DB-enforced last-admin invariant).

## Operational lessons
- **Supabase free-tier projects auto-pause after ~7 days of inactivity.** The **staging** project (`hunvbflchgjphnhdjmws`) was found paused on 2026-06-30 after a break; unpausing triggers a restore that takes a short while — queries during restore fail with `relation "<table>" does not exist` / `schema ... does not exist`. **Before resuming work after a break: unpause staging in the Supabase dashboard and confirm `status: ACTIVE_HEALTHY` (Management API `GET /v1/projects/<ref>`) before applying migrations or running verification.** Prod is on a paid plan / actively trafficked, so it does not pause.

## Process (non-negotiable)
- **TDD throughout**: write the failing test first → run it and watch it fail → minimal implementation → green → commit. One logical change per commit.
- **Load all superpowers skills** from `/mnt/skills/user/` and `/mnt/skills/public/` at the start of every session, and invoke the relevant skill before acting (brainstorming → writing-plans → subagent-driven-development, plus test-driven-development, systematic-debugging, etc.).
- **Approval gates before any side effect**: never apply a migration, deploy, or other outward-facing/irreversible action without explicit go-ahead. Flow is brainstorm → spec (approved) → plan (approved) → build → staging → prod.
- **Subagent-driven execution**: one fresh subagent per task, with two-stage review between tasks — spec-compliance review first, then code-quality review. Fix all review findings before moving to the next task.

## Branch & deploy workflow
- Branches flow `feat/xxx → staging → master`. **Never commit or deploy directly to master.**
- `master` = production (Vercel auto-deploys on push); the `staging` branch deploys to `staging.wherecaniwatchit.info`.
- **Migrations always staging-first**, verified, then a **separate explicit approval** before production. Never prod before staging.
- Account for Vercel propagation lag — verify against the actually-live deployment, not just a green build. Beware cache-masking: clear the relevant Redis key before a verification probe.

### Applying migrations safely (project refs + targeting)
- **Project refs:** staging = `hunvbflchgjphnhdjmws`, production = `ahgmszdrhndcycvairmn`.
- **DANGER — `supabase db push --db-url` can silently hit the WRONG project.** Supabase direct hosts (`db.<ref>.supabase.co`) no longer resolve — only the pooler does. When `--db-url` can't connect, the authenticated push falls back to the **CLI-linked project**, ignoring `--db-url`. On 2026-06-09 this applied the SP3 ranking migration to **prod instead of staging** because the CLI was linked to prod.
- **Preferred method — Management API with EXPLICIT ref** (no linking/DNS ambiguity): `POST https://api.supabase.com/v1/projects/<ref>/database/query` with `{ "query": "<sql>" }` and `Authorization: Bearer $SUPABASE_ACCESS_TOKEN`. Run the migration SQL, then record it: `insert into supabase_migrations.schema_migrations (version,name,statements) select '<version>','<name>',ARRAY[$mig$<sql>$mig$] where not exists (select 1 from supabase_migrations.schema_migrations where version='<version>')`.
- **Keep the CLI linked to STAGING** (`supabase link --project-ref hunvbflchgjphnhdjmws`) so any accidental `db push` targets staging, never prod.
- **Always verify the target after applying**: introspect the function/object on the intended ref (e.g. `select pg_get_functiondef('<fn>(...)'::regprocedure)`) AND confirm the OTHER ref is unchanged.

## Security
- **Never commit secrets, `.env` files, or credentials.** Secrets live only in gitignored `.env.local` / `.env.staging.local` and in Vercel env vars.
- **Rotate any DB token/password** shared into a session immediately after it has been used.

## Architecture decisions (this project)
- **Task N (PgBouncer / connection pooling) is DROPPED — not applicable.** The app uses Supabase via the PostgREST REST API (`@supabase/supabase-js`, `@supabase/ssr`), not a direct Postgres driver; Supabase pools PostgREST natively. Revisit only if a direct-pg path is ever added (e.g. a future ORM in the CMS).
- **Analytics: Vercel Analytics + Speed Insights adopted.** Cookieless, no PII. The Privacy Policy must disclose cookieless/aggregated/no-PII analytics — do not claim "zero tracking."
- **Redis keys are environment-namespaced** (`<env>:...`) on a shared Upstash instance, isolating staging from production cache + rate-limit buckets.
- **Search pipeline**: normalize → Postgres FTS (`websearch_to_tsquery`; weighted `tsvector` A/B/C/D = title/synopsis/genres/cast, **trigger-maintained**) → `pg_trgm` fuzzy (similarity ≥ 0.3, `%` operator for index use) → TMDB fallback + quota-gated on-demand seed. NOTE: a GENERATED tsvector column can't use `array_to_string` (non-immutable) — use a trigger instead.
- **Ranking tuning**: both an IMDb/popularity tie-break AND a supplementary-content soft penalty (down-rank "behind the scenes / making of / paleyfest / featurette / clip" etc., as a sort key above ts_rank) so canonical titles win ambiguous queries. Exact title match always ranks first.

## Pre-launch product decisions
- **Auth (SP6)**: invite-only for Contributors & Reviewers; Admin assigned manually; no self-registration.
- **Expanded flags (SP8)**: add structured columns `reported_platform` + `reported_watch_url` to the `flags` table (not freeform notes).
- **Contributor payments (SP7)**: ship the contribution counter + admin view first; the payment rate is deferred and set operationally later.
- **SP7 build tooling (decided 2026-07-06)**: Allen will build SP7 with **Fable 5** (not Claude Design + Claude Code). The SP7 technical spec must therefore be **Fable-ready** — include the full design-system tokens so Fable can build directly from it. **Tokens:** primary blue `#2B72E8`, background `#FFFFFF`, text `#171717`, muted `#717177`, success `#34C759`, error `#FF3B30`; fonts **Space Grotesk** (display/headings) + **DM Sans** (body); component library **shadcn/ui**.
- **SP7 scope (decided 2026-07-06)**: **Full CMS, all modules in one spec, built together** (Allen's call; higher risk flagged). Modules: (1) review queue + data-accuracy/confidence, (2) direct availability editor, (3) platform CRUD, (4) title management, (5) in-app user/invite management, (6) contribution counter + admin dashboard. **Role model = trust ladder:** contributor edits availability (writes land MEDIUM confidence / pending reviewer approval, earns contribution_count); reviewer = same tools but writes land HIGH confidence + can approve contributor writes; admin = everything + title metadata (admin-only) + platform CRUD + user/invite mgmt + all-user dashboard. **Flag-accept = pre-filled confirm** (opens availability row pre-filled from flag data, reviewer confirms → writes `source='reviewer'`, high confidence, resolves flag). **Confidence = enum high/medium/low** on availability. **Title mgmt = status-aware override:** add `titles.metadata_overrides jsonb` (store only overridden fields; re-sync merges non-overridden only), `titles.tmdb_id` → nullable (local PH content); warnings keyed off existing `titles.status` (ended/released = "edits persist" note; returning/in_production = "re-sync may overwrite" stronger warning).
- **Staging is permanently `noindex`** (independent of the prod pre-launch `SITE_INDEXABLE` toggle). Launch = flip `SITE_INDEXABLE` → true (prod only).

## SP5 — MOTN Philippines data-gap finding (2026-06-08)
- MOTN has a **systematic Disney+ PH gap**: 0/8 Disney+ PH titles returned availability (Netflix/Apple TV = 5/5 correct). Viu PH, iWantTFC, Vivamax, and WeTV PH are **entirely absent** from MOTN's PH service registry (MOTN indexes only 9 PH services).
- **Action: seed PH availability aggressively before launch** — priority Disney+ PH → Viu PH → iWantTFC → Vivamax. Full report: `docs/superpowers/research/2026-06-08-motn-ph-data-gap.md`.
- HIMYM is **confirmed on Disney+ PH** (personally verified 2026-06-09); the MOTN Disney+ PH gap is real and systematic, not a per-title fluke.
- **SP7 rule**: in the verification queue, any availability row where `platform = Disney+ PH` **and** the row came from the MOTN aggregator defaults to a **very low confidence score** (MOTN is known-unreliable for this platform/region). **IMPORTANT taxonomy correction (verified on prod 2026-07-06):** there is **no `source='motn'`** in the DB — MOTN-seeded rows are written `source='api'` ([lib/sync.ts:134](lib/sync.ts#L134)) and the daily cron writes `source='cron'` ([app/api/cron/sync-availability/route.ts:94](app/api/cron/sync-availability/route.ts#L94)). So the rule must key on **`source IN ('api','cron')` (aggregator) AND platform slug IN ('disney','disney-plus') AND `region_code='PH'`**, not a literal `motn` source. Human writes (`source IN ('reviewer','contributor','manual')`) always override to high confidence.

## Backlog (post-SP7, documented 2026-07-06)
- **SP11 — PH Platform Accuracy** (post-SP7 sub-project): (1) **Vivamax/VMX** and **iWantTFC/iWant** manual ingestion — not in MOTN at all; (2) **Disney+ PH = Hulu PH equivalence** — same catalog, system treats them separately causing missed matches; (3) **HBO Max + Viu PH partnership** — Max content available through Viu PH; Korean/Max catalogs overlap. Needs **both data ingestion AND a platform-equivalence mapping in the schema** (e.g. a `platform_equivalences` / alias table so equivalent platforms resolve to one catalog).
- **SP12 — Search Quality Polish** (known issue): (1) *"The Big Bang Theory"* search returns **Young Sheldon** — TBBT likely not seeded; Young Sheldon metadata references TBBT heavily as spin-off. Fix = seed TBBT + improve ranking to **penalize spin-off metadata matches**. (2) **Search suggestions** (real-time as-you-type) — post-launch feature.
- **Ops backlog — cron dead-aggregator alert (logged 2026-07-06, do NOT build yet):** make `sync-availability` cron log/alert when `refreshed=0 && stoppedForQuota=false` — that's the signature of "MOTN aggregator returned nothing usable" (silent no-op). Confirmed live on prod 2026-07-06: MOTN subscription inactive → cron burns ~43 quota calls/day but writes **0 `source='cron'` rows ever**; no error surfaced. Related: **95% of prod availability (8,665/9,120) is stale >30 days** (June-4 bulk seed); only the 447 `checker` rows stay fresh (MOTN-independent). Post-SP7 the manual availability editor + checkers are the freshness mechanism → supports the "cancel MOTN at SP7 ship" call.

## Data snapshot (prod `ahgmszdrhndcycvairmn`, 2026-07-06)
- **Titles: 3,494 · Availability rows: 9,120** (8,870 `available=true`). Source breakdown: `api` 8,673 (MOTN bulk seed), `checker` 447 (direct HTTP), everything else 0.
- **MOTN quota:** July 2026 = **43/25,000**, June = 3,487/25,000 — usage is trivial.
- **MOTN renewal ($43/mo) advice:** defensible to pause, but the clean cut is **after SP7 ships** (once the manual availability editor exists). What breaks if paused: daily `sync-availability` cron refresh + search on-demand seeding; what survives: TMDB metadata, the 5 regional checker crons, all existing rows. **PH gaps are not MOTN-solvable** (SP5/SP11), so MOTN adds nothing to the biggest accuracy problem. Recommendation on record: **renew one more cycle, cancel at SP7 ship.**
